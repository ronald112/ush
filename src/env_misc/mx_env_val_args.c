#include "ush.h"

bool mx_env_val_args(char *args) {
    char *arg = NULL;

    for (unsigned int i = 1; args[i]; ++i) {
        for (unsigned int j = 0; args[i][j] && args[i][j] != '='; ++j) {
            
        }
    }
}
