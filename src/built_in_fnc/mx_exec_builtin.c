#include "ush.h"

int mx_exec_builtin(int cdm_ind) {
    switch (cdm_ind) {
    case 1: // fallthrough
        exit(0);
    case 2:
        
        break;
    default:
        break;
    }
    return 1;
}
